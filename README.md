# Discord-Presence-API
Create a RESTful API and WebSocket service that exposes Discord user presence and activities to web applications. The service should provide both basic profile information via REST API and real-time presence updates via WebSocket Gateway connection.
